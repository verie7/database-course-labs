Server [localhost]:
Database [postgres]:
Port [5432]:
Username [postgres]:
Password for user postgres:
psql (17.6)
WARNING: Console code page (866) differs from Windows code page (1251)
         8-bit characters might not work correctly. See psql reference
         page "Notes for Windows users" for details.
Type "help" for help.

postgres=# CREATE DATABASE students_db;
CREATE DATABASE
postgres=# CREATE DATABASE university;
CREATE DATABASE
postgres=# \c students_db;
You are now connected to database "students_db" as user "postgres".
students_db=# \dt
Did not find any relations.
students_db=# \c university;
You are now connected to database "university" as user "postgres".
university=# DROP DATABASE university;
ОШИБКА:  удалить базу данных, открытую в данный момент, нельзя
university=# DROP DATABASE students_db;
DROP DATABASE
university=# \c postgres;
You are now connected to database "postgres" as user "postgres".
postgres=# DROP DATABASE university;
DROP DATABASE
postgres=# \q
